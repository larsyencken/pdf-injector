- After any change, run `make test` to check that it works
- Install new packages with `uv add <pkg>` or `uv add --dev <pkg>`
- Set up or refresh the virtualenv with `uv sync` (no additional arguments needed)